# Claude Code session state
.claude/

# Internal planning docs (kept local; not pushed)
docs/

# Python
__pycache__/
*.py[cod]
.venv/
.env

# Project artifacts (committed inputs only; outputs are reproducible)
data/chroma_db/
data/pdfs/
data/chunks.jsonl

# Eval per-case JSONL — only the rendered eval-report.md is committed
reports/eval_results_*.jsonl

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/

# OS
.DS_Store
